diff --git a/src/big_digit.rs b/src/big_digit.rs
index 24bf4b5..adf5fa3 100644
--- a/src/big_digit.rs
+++ b/src/big_digit.rs
@@ -2,8 +2,8 @@ use alloc::vec::Vec;
 
 // A [`BigDigit`] is a [`BigUint`]'s composing element.
 cfg_digit!(
-    pub(crate) type BigDigit = u32;
-    pub(crate) type BigDigit = u64;
+    pub type BigDigit = u32;
+    pub type BigDigit = u64;
 );
 
 // A [`DoubleBigDigit`] is the internal type used to do the computations.  Its
@@ -41,7 +41,7 @@ pub(crate) fn to_doublebigdigit(hi: BigDigit, lo: BigDigit) -> DoubleBigDigit {
     DoubleBigDigit::from(lo) | (DoubleBigDigit::from(hi) << BITS)
 }
 
-pub(crate) enum BigDigits {
+pub enum BigDigits {
     Inline(Option<BigDigit>),
     Heap(Vec<BigDigit>),
 }
diff --git a/src/bigint.rs b/src/bigint.rs
index dcf1d74..acc347c 100644
--- a/src/bigint.rs
+++ b/src/bigint.rs
@@ -519,7 +519,7 @@ impl Roots for BigInt {
             n
         );
 
-        BigInt::from_biguint(self.sign, self.data.nth_root(n))
+        BigInt::from_biguint(self.sign, self.data.nth_root(n.into()))
     }
 
     fn sqrt(&self) -> Self {
@@ -1139,8 +1139,14 @@ impl BigInt {
 
     /// Returns the truncated principal `n`th root of `self` --
     /// See [`num_integer::Roots::nth_root()`].
-    pub fn nth_root(&self, n: u32) -> Self {
-        Roots::nth_root(self, n)
+    pub fn nth_root(&self, n: u64) -> Self {
+        assert!(
+            !(self.is_negative() && n.is_even()),
+            "root of degree {} is imaginary",
+            n
+        );
+
+        BigInt::from_biguint(self.sign, self.data.nth_root(n))
     }
 
     /// Returns the number of least-significant bits that are zero,
diff --git a/src/biguint.rs b/src/biguint.rs
index fd7d1a6..b0d032c 100644
--- a/src/biguint.rs
+++ b/src/biguint.rs
@@ -30,6 +30,8 @@ mod shift;
 
 pub(crate) use self::convert::to_str_radix_reversed;
 pub use self::iter::{U32Digits, U64Digits};
+pub use multiplication::mac3;
+pub use division::{div_rem_digit, rem_digit};
 
 /// Find last set bit
 /// fls(0) == 0, fls(u32::MAX) == 32
@@ -387,65 +389,7 @@ impl Roots for BigUint {
     // Reference:
     // Brent & Zimmermann, Modern Computer Arithmetic, v0.5.9, Algorithm 1.14
     fn nth_root(&self, n: u32) -> Self {
-        assert!(n > 0, "root degree n must be at least 1");
-
-        if self.is_zero() || self.is_one() {
-            return self.clone();
-        }
-
-        match n {
-            // Optimize for small n
-            1 => return self.clone(),
-            2 => return self.sqrt(),
-            3 => return self.cbrt(),
-            _ => (),
-        }
-
-        // The root of non-zero values less than 2ⁿ can only be 1.
-        let bits = self.bits();
-        let n64 = u64::from(n);
-        if bits <= n64 {
-            return BigUint::ONE;
-        }
-
-        // If we fit in `u64`, compute the root that way.
-        if let Some(x) = self.to_u64() {
-            return x.nth_root(n).into();
-        }
-
-        let max_bits = bits / n64 + 1;
-
-        #[cfg(feature = "std")]
-        let guess = match self.to_f64() {
-            Some(f) if f.is_finite() => {
-                use num_traits::FromPrimitive;
-
-                // We fit in `f64` (lossy), so get a better initial guess from that.
-                BigUint::from_f64((f.ln() / f64::from(n)).exp()).unwrap()
-            }
-            _ => {
-                // Try to guess by scaling down such that it does fit in `f64`.
-                // With some (x * 2ⁿᵏ), its nth root ≈ (ⁿ√x * 2ᵏ)
-                let extra_bits = bits - (f64::MAX_EXP as u64 - 1);
-                let root_scale = Integer::div_ceil(&extra_bits, &n64);
-                let scale = root_scale * n64;
-                if scale < bits && bits - scale > n64 {
-                    (self >> scale).nth_root(n) << root_scale
-                } else {
-                    BigUint::ONE << max_bits
-                }
-            }
-        };
-
-        #[cfg(not(feature = "std"))]
-        let guess = BigUint::ONE << max_bits;
-
-        let n_min_1 = n - 1;
-        fixpoint(guess, max_bits, move |s| {
-            let q = self / s.pow(n_min_1);
-            let t = n_min_1 * s + q;
-            t / n
-        })
+        self.nth_root(n.into())
     }
 
     // Reference:
@@ -1004,8 +948,65 @@ impl BigUint {
 
     /// Returns the truncated principal `n`th root of `self` --
     /// see [Roots::nth_root](https://docs.rs/num-integer/0.1/num_integer/trait.Roots.html#tymethod.nth_root).
-    pub fn nth_root(&self, n: u32) -> Self {
-        Roots::nth_root(self, n)
+    pub fn nth_root(&self, n: u64) -> Self {
+        assert!(n > 0, "root degree n must be at least 1");
+
+        if self.is_zero() || self.is_one() {
+            return self.clone();
+        }
+
+        match n {
+            // Optimize for small n
+            1 => return self.clone(),
+            2 => return self.sqrt(),
+            3 => return self.cbrt(),
+            _ => (),
+        }
+
+        // The root of non-zero values less than 2ⁿ can only be 1.
+        let bits = self.bits();
+        if bits <= n {
+            return BigUint::ONE;
+        }
+
+        // If we fit in `u64`, compute the root that way.
+        if let Some(x) = self.to_u64() {
+            return x.nth_root(n as u32).into();
+        }
+
+        let max_bits = bits / n + 1;
+
+        #[cfg(feature = "std")]
+        let guess = match self.to_f64() {
+            Some(f) if f.is_finite() => {
+                use num_traits::FromPrimitive;
+
+                // We fit in `f64` (lossy), so get a better initial guess from that.
+                BigUint::from_f64((f.ln() / (n as f64)).exp()).unwrap()
+            }
+            _ => {
+                // Try to guess by scaling down such that it does fit in `f64`.
+                // With some (x * 2ⁿᵏ), its nth root ≈ (ⁿ√x * 2ᵏ)
+                let extra_bits = bits - (f64::MAX_EXP as u64 - 1);
+                let root_scale = Integer::div_ceil(&extra_bits, &n);
+                let scale = root_scale * n;
+                if scale < bits && bits - scale > n {
+                    (self >> scale).nth_root(n) << root_scale
+                } else {
+                    BigUint::ONE << max_bits
+                }
+            }
+        };
+
+        #[cfg(not(feature = "std"))]
+        let guess = BigUint::ONE << max_bits;
+
+        let n_min_1 = n - 1;
+        fixpoint(guess, max_bits, move |s| {
+            let q = self / Pow::pow(s, n_min_1);
+            let t = n_min_1 * s + q;
+            t / n
+        })
     }
 
     /// Returns the number of least-significant bits that are zero,
@@ -1095,7 +1096,7 @@ impl num_traits::ToBytes for BigUint {
     }
 }
 
-pub(crate) trait IntDigits {
+pub trait IntDigits {
     fn digits(&self) -> &[BigDigit];
     fn digits_mut(&mut self) -> &mut BigDigits;
     fn normalize(&mut self);
diff --git a/src/biguint/division.rs b/src/biguint/division.rs
index b414696..e02f502 100644
--- a/src/biguint/division.rs
+++ b/src/biguint/division.rs
@@ -84,7 +84,7 @@ fn div_half(rem: BigDigit, digit: BigDigit, divisor: BigDigit) -> (BigDigit, Big
 }
 
 #[inline]
-pub(super) fn div_rem_digit(mut a: BigUint, b: BigDigit) -> (BigUint, BigDigit) {
+pub fn div_rem_digit(mut a: BigUint, b: BigDigit) -> (BigUint, BigDigit) {
     if b == 0 {
         panic!("attempt to divide by zero")
     }
@@ -110,7 +110,7 @@ pub(super) fn div_rem_digit(mut a: BigUint, b: BigDigit) -> (BigUint, BigDigit)
 }
 
 #[inline]
-fn rem_digit(a: &BigUint, b: BigDigit) -> BigDigit {
+pub fn rem_digit(a: &BigUint, b: BigDigit) -> BigDigit {
     if b == 0 {
         panic!("attempt to divide by zero")
     }
diff --git a/src/biguint/multiplication.rs b/src/biguint/multiplication.rs
index dbdb31b..542e5b5 100644
--- a/src/biguint/multiplication.rs
+++ b/src/biguint/multiplication.rs
@@ -65,7 +65,7 @@ fn bigint_from_slice(slice: &[BigDigit]) -> BigInt {
 /// Three argument multiply accumulate:
 /// acc += b * c
 #[allow(clippy::many_single_char_names)]
-fn mac3(mut acc: &mut [BigDigit], mut b: &[BigDigit], mut c: &[BigDigit]) {
+pub fn mac3(mut acc: &mut [BigDigit], mut b: &[BigDigit], mut c: &[BigDigit]) {
     // Least-significant zeros have no effect on the output.
     if let Some(&0) = b.first() {
         if let Some(nz) = b.iter().position(|&d| d != 0) {
diff --git a/src/lib.rs b/src/lib.rs
index 771a27c..17acf12 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -227,6 +227,7 @@ impl<T> fmt::Display for TryFromBigIntError<T> {
 
 pub use crate::biguint::BigUint;
 pub use crate::biguint::ToBigUint;
+pub use crate::biguint::IntDigits;
 pub use crate::biguint::U32Digits;
 pub use crate::biguint::U64Digits;
 
@@ -234,6 +235,8 @@ pub use crate::bigint::BigInt;
 pub use crate::bigint::Sign;
 pub use crate::bigint::ToBigInt;
 
+pub use crate::biguint::{div_rem_digit, mac3, rem_digit};
+
 #[cfg(feature = "rand_core_0_10")]
 pub use crate::bigrand::BigRng010;
 
@@ -244,3 +247,5 @@ pub use crate::bigrand::BigRng09;
 pub use crate::bigrand::{RandomBits, UniformBigInt, UniformBigUint};
 
 mod big_digit;
+
+pub use crate::big_digit::{BigDigit, BigDigits};
